#include<stdio.h>
int main()
{
    //this is average of five nu.s only we can do for more 
    int a,b,c,d,e,sum,avrg;
    printf("Enter five numbers");
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    sum=a+b+c+d+e;
    avrg=sum/5;
    printf("The avergae of numbers are %d",avrg);
    return 0;
}
