#include<stdio.h>
int main()
{
    int n,fact=1,i;
    printf("Enter a number");
    scanf("%d",&n);
    for(i=n;i>0;i--)
    fact=fact*i;
    printf("%d is the factorial of %d",fact,n);
    return 0;
}
